# Product Debt Tracker

产品债只记录仍需要业务、课程要求或用户决策才能确认的活跃 open 问题；已完成、已明确不做或已写入规格边界的事项不在本表保留。

Next PD ID: `PD-019`

维护提示：新增产品债时使用 `Next PD ID` 的编号；写入新行后立刻把 `Next PD ID` 自增 1。已删除、已收口或历史引用过的编号不复用。

| ID | Area | Question | Needed Decision | Status |
| --- | --- | --- | --- | --- |
| PD-003 | Inventory Alert | 当前库存监控中的低储、高储、缺货和呆滞风险阈值与展示口径仍未冻结 | 确认阈值来源、全局/物料级优先级、页面仅提示还是影响业务操作 | open |
| PD-005 | Packaging | 容器类型已进入单据和库存标签，但容器实例、装箱、拆箱、合箱是否进入业务闭环仍未定 | 确认是否只保留 `container_type` + 箱数/件数，还是新增容器实例和拆合箱流程 | open |
| PD-006 | Scan Audit | 浏览器扫码失败、设备日志、PDA 操作日志是否需要持久化 | 确认是否仅保留前端错误提示，还是新增 `scan_attempt_log` 记录失败原因、设备、操作人和原始码 | open |
| PD-008 | Master Data | 供应商、物料、仓库、库位现有基础资料维护边界是否足够支撑后续验收 | 确认是否补充导入导出、启停用引用保护、校验规则和权限边界 | open |
| PD-014 | Outbound FIFO | Week4 仅实现“普通按单出库时阻断非最早剩余库存标签”，但对跨单补锁、人工改配后是否允许提示后继续、以及无单强制出库是否仍显示 FIFO 提示尚未定稿 | 确认 FIFO 在普通出库、强制出库、改配/补锁场景下是“硬阻断”还是“提示并允许继续”，以及前端是否需要例外授权流程 | open |
| PD-017 | FR-08/FR-09 Rework | 表格批量导入和 AI 预警在 Week4 验收中为跳过/待返工，后续返工范围尚未冻结 | 确认 FR-08 是否作为独立表格导入重做，FR-09 是规则型风险预警、智能推荐展示，还是继续仅保留数据准备 | open |
| PD-018 | Auto Location Allocation | Iteration 5 只做人工批量设置库位，不做按库位容量、混放规则、物料限制或优先级自动分配库位 | 确认是否需要建立库位容量与物料约束模型，并定义自动推荐或自动分配策略 | open |
